const d3 = require('d3');

function resize(result:any, width:number, height:number, spec:any){
    // Adjust width and height of the chart
    // d3.select("svg").attr('width', width).attr('height', height);

}

export {resize}